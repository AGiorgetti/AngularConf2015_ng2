// Place your settings in this file to overwrite default and user settings.
{
	// VSCode is set to use typescript 1.6 internally, to use a newer version:
	
	// If the version of the compiler used locally differs from the one internally used you need to change
	// this setting.
	// Specifies the folder path containing the tsserver and lib*.d.ts files to use.
	//"typescript.tsdk": "C:\\Work\\AngularConf2015\\AngularConf2015\\node_modules\\typescript\\lib"
	
	// relative path is not working
	//"typescript.tsdk": "${workspaceRoot}\\node_modules\\typescript\\lib"
	//"typescript.tsdk": "node_modules/typescript/lib"
	
	// JavaScript
	
	// Always include all words from the current document.
	//"javascript.suggest.alwaysAllWords": true

	// Complete functions with their parameter signature.
	//"javascript.suggest.useCodeSnippetsOnMethodSuggest": true
}